interface:
  display_name: "Mermail Bounty Intake Agent"
  short_description: "Rank paid-work emails by verified, risk-adjusted expected value"
  default_prompt: "Use $mermail-bounty-intake-agent to review my Mermail paid-work opportunities safely and rank them by risk-adjusted expected value."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
